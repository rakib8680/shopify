import React from 'react';

const TShirt = ({ tshirt }) => {
    const { name, picture, _id, price, gender } = tshirt


    console.log(tshirt)
    return (
        <div>

        </div>
    );
};

export default TShirt;